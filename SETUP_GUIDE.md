'''
# 🚀 Пошаговая инструкция по настройке на iPad

Это руководство поможет вам развернуть и настроить систему автопостинга, используя только iPad и браузер.

## 🎯 Цель

Развернуть Cloud Functions и настроить Firebase, чтобы система работала автоматически в облаке.

## 🛠️ Инструменты

- **iPad** с доступом в интернет
- **Браузер** (Safari или Chrome)
- **Аккаунт GitHub**
- **Аккаунт Firebase** (с платным планом Blaze)

---

## ⚙️ Шаг 1: Открытие проекта в GitHub Codespaces

Codespaces — это полноценная среда разработки прямо в вашем браузере. Это позволит нам использовать терминал для установки и развертывания Firebase.

1.  **Откройте репозиторий на GitHub:**
    Перейдите по ссылке: [https://github.com/lolaperfect999-art/autopost-firebase](https://github.com/lolaperfect999-art/autopost-firebase)

2.  **Запустите Codespace:**
    - Нажмите зеленую кнопку **`< > Code`**.
    - Перейдите на вкладку **`Codespaces`**.
    - Нажмите **`Create codespace on main`**.
    - Подождите 1-2 минуты, пока Codespace создается. У вас откроется полноценный VS Code прямо в браузере.

![Step 1: Create Codespace](https://i.imgur.com/8l1bOaB.png)

---

## ⚙️ Шаг 2: Установка и авторизация Firebase CLI

Теперь, когда у вас есть терминал, мы можем установить Firebase Command Line Interface (CLI).

1.  **Откройте терминал:**
    Внизу экрана вы увидите вкладку **`TERMINAL`**. Если ее нет, откройте ее через меню: `☰` (вверху слева) → `Terminal` → `New Terminal`.

2.  **Установите Firebase CLI:**
    Вставьте в терминал следующую команду и нажмите Enter:

    ```bash
    npm install -g firebase-tools
    ```

    Установка займет около минуты.

3.  **Авторизуйтесь в Firebase:**
    Теперь нам нужно связать этот терминал с вашим аккаунтом Firebase.

    - Введите команду:

      ```bash
      firebase login --no-localhost
      ```

    - В терминале появится ссылка. Скопируйте ее.
    - Откройте эту ссылку в новой вкладке браузера.
    - Выберите ваш Google аккаунт, связанный с Firebase.
    - Разрешите доступ.
    - Скопируйте код авторизации, который появится на экране.
    - Вернитесь в Codespace и вставьте этот код в терминал.

    Если все прошло успешно, вы увидите сообщение `✔ Success! Logged in as your@email.com`.

![Step 2: Firebase Login](https://i.imgur.com/O3g4c8f.png)

---

## ⚙️ Шаг 3: Развертывание проекта

Это самый важный шаг. Мы загрузим весь код (Cloud Functions, правила безопасности) в ваш проект Firebase.

1.  **Проверьте проект:**
    Убедитесь, что вы находитесь в правильной директории. В терминале должно быть написано что-то вроде `ubuntu@codespaces-xxxx:/workspaces/autopost-firebase`.

2.  **Выполните команду развертывания:**
    Введите в терминал следующую команду и нажмите Enter:

    ```bash
    firebase deploy
    ```

    Процесс займет 2-5 минут. Он сделает следующее:
    - Загрузит правила для Firestore и Storage.
    - Создаст индексы в Firestore.
    - Установит все Python зависимости.
    - Развернет 4 Cloud Functions (`check_scheduled_posts`, `create_post`, `get_posts`, `retry_failed_post`).

    В конце вы должны увидеть `✔ Deploy complete!`.

![Step 3: Firebase Deploy](https://i.imgur.com/D9y2e7g.png)

---

## ⚙️ Шаг 4: Настройка Cloud Scheduler

Теперь нам нужно настроить автоматический запуск функции `check_scheduled_posts` каждую минуту.

1.  **Перейдите в Google Cloud Console:**
    Откройте [https://console.cloud.google.com/](https://console.cloud.google.com/) и убедитесь, что выбран ваш проект (`autopost-syste`).

2.  **Найдите Cloud Scheduler:**
    Используйте строку поиска вверху и введите `Cloud Scheduler`.

3.  **Включите API:**
    Если потребуется, нажмите `Enable API`.

4.  **Проверьте задание:**
    Вы должны увидеть задание с именем `firebase-schedule-check_scheduled_posts-us-central1`. Это означает, что все настроено правильно!

    - **State:** `Enabled`
    - **Frequency:** `* * * * *` (каждую минуту)
    - **Target:** `Cloud Pub/Sub`

    Если задание есть и оно включено, **ничего больше делать не нужно**. Система уже работает!

---

## 🎉 Поздравляю! Ваша система автопостинга готова!

Теперь все работает автоматически в облаке. Вам больше не нужен Codespaces (вы можете его закрыть).

## 📝 Как управлять системой (через iPad)

Все управление происходит через веб-консоль Firebase.

### 1. Добавление аккаунта xfree.com

1.  Откройте [Firebase Console](https://console.firebase.google.com/).
2.  Перейдите в **Firestore Database**.
3.  Нажмите **`+ Start collection`**.
4.  **Collection ID:** `accounts`
5.  Нажмите **`+ Add document`**.
6.  Нажмите **`Auto-ID`** для автоматической генерации ID.
7.  Добавьте поля:
    - `email` (String): `your_xfree_email@example.com`
    - `password` (String): `your_xfree_password`
    - `platform` (String): `xfree`
    - `status` (String): `active`
    - `proxy` (String): (оставьте пустым, если нет прокси)
8.  Нажмите **`Save`**. Скопируйте **Document ID** этого аккаунта, он понадобится для создания постов.

### 2. Загрузка видео

1.  В Firebase Console перейдите в **Storage**.
2.  Нажмите **`Upload file`**.
3.  Выберите видео с вашего iPad.
4.  После загрузки, нажмите на файл и скопируйте **`gs://` URL** (например, `gs://autopost-syste.appspot.com/your-video.mp4`).

### 3. Создание запланированного поста

1.  Перейдите в **Firestore Database**.
2.  Нажмите **`+ Start collection`** (если ее нет).
3.  **Collection ID:** `posts`
4.  Нажмите **`+ Add document`**.
5.  Нажмите **`Auto-ID`**.
6.  Добавьте поля:
    - `account_id` (String): ID документа аккаунта из шага 1.
    - `video_url` (String): `gs://` URL видео из шага 2.
    - `title` (String): `Мой новый пост`
    - `description` (String): `Описание для поста.`
    - `scheduled_time` (Timestamp): Выберите дату и время публикации.
    - `status` (String): `pending`
    - `platform` (String): `xfree`
7.  Нажмите **`Save`**.

**Готово!** В указанное время Cloud Function автоматически опубликует этот пост.

### 4. Просмотр логов и статуса

-   **Статус поста:** В коллекции `posts` поле `status` изменится на `processing`, затем на `published` или `failed`.
-   **Логи:** В Firebase Console перейдите в **Functions** → **Logs**, чтобы увидеть, что происходит.

---

Если у вас возникнут вопросы на любом из этапов — просто спросите! Я готов помочь. 😊
'''
